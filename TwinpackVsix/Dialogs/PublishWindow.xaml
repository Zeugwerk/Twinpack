<Window x:Class="Twinpack.Dialogs.PublishWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Twinpack.Dialogs" d:DataContext="{d:DesignInstance Type=local:PublishWindow}"
             mc:Ignorable="d"
             ResizeMode="NoResize"
             WindowStartupLocation="CenterScreen"
             Height="520" Width="462">
    <Window.Resources>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:BooleanToInverseBooleanConverter x:Key="BooleanToInverseBooleanConverter"/>
        <local:UrlToImageConverter x:Key="UrlToImageConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.Background>
                <SolidColorBrush Color="#FFcad0de"/>
            </Grid.Background>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" Height="50" Source="Images/Twinpack.png" HorizontalAlignment="Center" VerticalAlignment="Center"></Image>
            <TextBlock Grid.Column="1" Text="Twinpack Edit Package" Visibility="{Binding IsNewPackage, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='inverse'}" TextWrapping="Wrap" Margin="0,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="14" FontFamily="Menrope" FontWeight="DemiBold" Foreground="#FF2A334B"/>
            <TextBlock Grid.Column="1" Text="Twinpack Publish Package" Visibility="{Binding IsNewPackage, Converter={StaticResource BooleanToVisibilityConverter}}" TextWrapping="Wrap" Margin="0,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="14" FontFamily="Menrope" FontWeight="DemiBold" Foreground="#FF2A334B"/>
        </Grid>

        <!-- General Data -->
        <StackPanel Grid.Row="1" Orientation="Vertical"  Margin="5 15 0 0">
            <Label Content="Name:" HorizontalAlignment="Left" Margin="0"/>
            <TextBlock Text="{Binding PackageName}" HorizontalAlignment="Center" Margin="0" FontSize="18" FontWeight="SemiBold" />
             
            <Label Content="Distributor name:" HorizontalAlignment="Left" Margin="0"/>
            <TextBlock Text="{Binding DistributorName}" Visibility="{Binding IsNewPackage, Converter={StaticResource BooleanToVisibilityConverter},ConverterParameter='inverse'}"/>
            <TextBox Text="{Binding DistributorName}" Width="200" Height="100" Margin="0" Visibility="{Binding IsNewPackage, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <Border Width="128" Height="128" Grid.Row="0" Background="#DA776D" CornerRadius="64" Margin="0 10 0 0">
                <Image Source="{Binding ImageIcon}" Width="128" Height="128" Margin="0,0,0,10"/>
            </Border>

            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnEditPackage"  Click="EditPackage_Click" Content="Edit Package" Width="80" Margin="5" HorizontalAlignment="Center"
                    Visibility="{Binding IsEditPackageVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <Button Click="ChangeIcon_Click" Background="#f5f1ee" BorderThickness="0" Margin="0 -25 0 0" FontWeight="SemiBold" Foreground="#4757b8"
                        HorizontalAlignment="Left" Content="Change Icon" VerticalAlignment="Top" Width="80"
                        Visibility="{Binding IsGeneralDataReadOnly, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='inverse'}"  />
            </StackPanel>

            <StackPanel Orientation="Vertical" IsEnabled="{Binding IsGeneralDataReadOnly, Converter={StaticResource BooleanToInverseBooleanConverter}}">
                <Label Content="Display Name:" HorizontalAlignment="Left" Margin="0"/>
                <TextBox Text="{Binding DisplayName}" Width="200" Margin="0"/>

                <Label Content="Description:" HorizontalAlignment="Left" Margin="0"/>
                <TextBox Text="{Binding Description}" Width="200" Margin="0"/>

                <Label Content="Project URL:" HorizontalAlignment="Left" Margin="0"/>
                <TextBox Text="{Binding ProjectUrl}" Width="200" Margin="0"/>

                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="0 10 0 0" Visibility="{Binding IsApplyApplicable, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='inverse'}">
                    <Button Content="Apply" Width="50" Margin="0 5 0 5" Click="ApplyPackage_Click" HorizontalAlignment="Center" IsEnabled="{Binding IsApplyEnabled}"/>
                    <Button Content="Cancel" Width="50" Margin="5 5 0 5" Click="CancelPackage_Click" HorizontalAlignment="Center" IsEnabled="{Binding IsApplyEnabled}"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>

        <!-- Version Data -->
        <StackPanel Orientation="Vertical" Grid.Row="1" Grid.Column="2" Margin="10 15 5 5" IsEnabled="{Binding IsVersionDataReadOnly, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='inverse'}">

            <Label Content="Version:" HorizontalAlignment="Left" Margin="0"/>
            <TextBlock Text="{Binding Version}" HorizontalAlignment="Center" FontSize="16" FontWeight="SemiBold"  Visibility="{Binding IsVersionEditable, Converter={StaticResource BooleanToVisibilityConverter},ConverterParameter='inverse'}"/>
            <TextBox Text="{Binding Version}" Width="200" Margin="0" TextChanged="VersionTextBox_TextChanged" Visibility="{Binding IsVersionEditable, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <Label Content="License File:" HorizontalAlignment="Left" Margin="0"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding LicenseFile}" Width="176" Margin="0"/>
                <Button Content="..." Width="25" Margin="-1 0 0 0" Click="Apply_Click" HorizontalAlignment="Center" IsEnabled="{Binding IsApplyEnabled}"/>
            </StackPanel>

            <Label Content="Authors:" HorizontalAlignment="Left" Margin="0"/>
            <TextBox Text="{Binding Authors}" Width="200" Height="50" Margin="0"/>

            <Label Content="Notes:" HorizontalAlignment="Left" Margin="0"/>
            <TextBox Text="{Binding Notes}" Width="200" Height="100" Margin="0"/>
        </StackPanel>

        <!-- Publish Button -->
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button HorizontalAlignment="Right" Content="Publish" Width="80" Margin="5" Click="Publish_Click" IsEnabled="{Binding IsNewPackageVersion}" Visibility="{Binding IsVersionEditable, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <Button HorizontalAlignment="Right" Content="Apply" Width="80" Margin="5" Click="Apply_Click" Visibility="{Binding IsNewPackage, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='inverse'}"/>
            <Button HorizontalAlignment="Right" Content="Close" Width="80" Margin="5" Click="Close_Click"/>
        </StackPanel>

    </Grid>
</Window>
